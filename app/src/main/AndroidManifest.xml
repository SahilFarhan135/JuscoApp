<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tatatest">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <supports-screens
        android:largeScreens="true"
        android:normalScreens="true"
        android:smallScreens="true"
        android:xlargeScreens="true" />

    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".About"></activity>
        <activity android:name=".lunch_2" />
        <activity android:name=".AttendanceHome" />
        <activity android:name=".LunchDisplay2" />
        <activity android:name=".Lunch_Submit" />
        <activity android:name=".Lunch_List" />
        <activity android:name=".BreakFast" />
        <activity android:name=".LunchHome" />
        <activity android:name=".EmployeeHome" />
        <activity android:name=".VisitorDisplay2" />
        <activity android:name=".Visitor_display" />
        <activity android:name=".Visitor_Home" />

        <service
            android:name=".LocationService"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".Admin_table3" />
        <activity android:name=".Admin_table2" />
        <activity android:name=".Admin_table" />
        <activity android:name=".Admin" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Forget_Email" />
        <activity android:name=".Forget" />
        <activity android:name=".BreakFast_Display" />
        <activity android:name=".Lunch_Breakfast" />
        <activity android:name=".TopNav_Map" />
        <activity android:name=".Lunch" />
        <activity android:name=".LunchDisplay1" />
        <activity android:name=".Appointment" />
        <activity android:name=".VisitorMain2" />
        <activity android:name=".VisitorMain" />
        <activity android:name=".VisitorSignup" />
        <activity android:name=".VisitorLogin" />
        <activity android:name=".EmployeeSignup" />
        <activity android:name=".EmployeeLogin" />
        <activity android:name=".MapsActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <uses-library android:name="com.google.android.maps" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity
            android:name=".Logo"
            android:label="@string/title_activity_maps">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>